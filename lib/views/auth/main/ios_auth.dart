import 'package:flutter/cupertino.dart';

class IosAuth extends StatelessWidget {
  const IosAuth({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
