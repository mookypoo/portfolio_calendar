import '../models/selected_month.dart';

class CalendarService {
  // void prevMonth({SelectedMonth month}){
  //   final int _year = month == 1 ? year - 1 : year;
  //   final int _month = selectedMonth == 1 ? 12 : selectedMonth - 1;
  //   selectedMonth.changeMonth(month: _month, year: _year);
  //   return;
  // }
}

